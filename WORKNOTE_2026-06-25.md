# 작업노트 — 2026-06-25

> ckpt-prep-stack 개발 세션 정리 (Claude Code)
> 브랜치: `dev` · 시작 커밋 `e70a111` → 종료 커밋 `443a607`(+ 본 세션 정리 커밋)

---

## 1. 이번 세션에서 한 일 (요약)

| # | 작업 | 상태 |
|---|---|---|
| 1 | SR 파싱 버그 수정 (경로 중복/REFILE 혼입) | ✅ 완료 (`56207c4`) |
| 2 | METAR/게이트/NOTAM AI를 공용 VPS 프록시로 전환 | ✅ 완료 (`9264f0d`) |
| 3 | RTE SUMMARY → PRIMARY/ALTERNATE RTEs 개편 | ✅ 완료 (`443a607`) |
| 4 | MEMO(NOTEPAD/DRAWPAD) 멀티페이지 추가 | ✅ 완료 (`443a607`) |
| 5 | FUEL&LOAD (PAX 분리/CGO 톤/연료 통계) | ✅ 완료 (`443a607`) |
| 6 | NOTAM 3개 페이지 경고문 추가 | ✅ 완료 (`443a607`) |
| 7 | 코드 검토 및 정리 (본 세션) | ✅ 완료 |

---

## 2. SR 파싱 버그 수정

- **증상:** 전체 경로가 2회 반복, REFILE 루트(ADNAP→RJTT)가 메인 루트에 혼입.
- **원인:** SR/터뷸런스 파싱 루프가 `fullText` 전체를 순회 → 페이지 88+의 CFP 복사본과 REFILE 섹션까지 읽음.
- **수정:** 루프에 `REFILE FLT PLAN` / `END OF JEPPESEN` / `ROUTE TO ALTN` 마커 만나면 `break`.
- **검증:** 테스트 OFP에서 웨이포인트 134개(중복) → 67개(정상).

---

## 3. 공용 VPS 프록시 (가장 큰 구조 변경)

조종사마다 API 키를 직접 입력해야 하는 접근성 문제를 해결.

- **엔드포인트:** `https://airbus380cbt.com/ckpt-proxy/` (Cloudflare HTTPS → nginx → Docker `127.0.0.1:8765`)
- **라우트:** `/metar` `/gate` `/notam-summary` `/health`
- **비밀 키:** VPS `/root/ckpt-proxy/.env` 에만 (`ANTHROPIC_API_KEY`, `AERODATABOX_KEY`) — git/클라이언트 비노출
- **부수 효과:** aviationweather.gov의 CORS 미지원으로 브라우저에서 항상 실패하던 METAR 조회가 프록시 경유로 해결됨
- **CORS:** nginx `map $http_origin`으로 `flyingjet777.github.io` + `localhost:8080`/`127.0.0.1` 허용
- **앱 변경:** 클라이언트의 키 입력 prompt/저장 코드(`getOrAskApiKey` 등) 전부 제거, `CKPT_PROXY_BASE` 경유로 통일
- **운영 참고:** 키 교체 시 VPS `.env` 수정 후 `docker restart ckpt-proxy`. VPS엔 hermes·n8n·A380 CBT 동시 가동 중(포트 충돌 주의).

---

## 4. RTE SUMMARY 개편 (PRIMARY / ALTERNATE)

- 탭명 변경: DATABASE/PILOT STORED → **PRIMARY / ALTERNATE RTEs**
- **ALTERNATE RTEs** 신규 표시 항목:
  - `ROUTE TO ALTN` (`..` = DCT로 분해, DEST 공항 제외)
  - **LOLV ETP** — 4자리 ICAO 구간만, 최대 4개, 좌표 중복 제거(`PUSH-BACK` 등 오매칭 차단), 위경도 10진 표기(`N4058.2`)
  - **3% CONTINGENCY ERA VALIDATION** — ERA 공항 + COC 좌표
  - **REFILE FLT PLAN** — 좌/우 컬럼 분리 파싱, 구간별 RQRD(klbs), PLANNED R/F, RIF 루트
- **OFP↔ATS FPL 비교:** `(FPL-편명-I…)` 블록 2개의 출발/루트/도착 비교 → PRIMARY 탭 하단에 일치/불일치 문구
- 레이아웃: PRIMARY·ALTERNATE 모두 13행 고정 + ▼▼/▲▲ 페이지네이션(스크롤 제거)

---

## 5. MEMO (메인 탭 DATA → MEMO)

- **NOTEPAD:** 멀티페이지 텍스트(자동 저장, 페이지 ◀▶ / +NEW PAGE)
- **DRAWPAD:** 캔버스 자유 필기(색 5종·펜 얇게/두껍게·지우개·전체삭제), 멀티페이지, 마우스+터치(펜슬)
- 저장: `localStorage`(`ckpt_memo_notepad_pages`/`ckpt_memo_drawpad_pages`) — 기기별
- ACTIVE·MEMO 버튼은 항상 흰색 테두리

---

## 6. FUEL & LOAD

- **PAX NBR:** 비즈니스/이코노미/총원 (FIRST는 BUSINESS에 합산)
- **CGO (T):** `CARGO: nnnn LBS` → 톤(/2000) 변환, MODE 칸 대체
- **ROUTE FUEL CONSUMPTION STATISTICS:** DISPATCH NOTES 아래 `MEAN/95%/99% = ±n/±n/±n (LBS)` (부호 필수)

---

## 7. NOTAM

- DEP/ARR · ALTN/EDTO · ERA/FIR 세 페이지 상단에 빨강 경고 `<중요> OFP NOTAM PACKAGE를 참고하세요`
- ERA WX의 TAF 인식 정규식이 `TAFHNL`처럼 공백 누락 케이스를 놓치던 버그 수정(`/^\s*TAF/`)
- *NOTAM 고도화는 다음 세션 예정*

---

## 8. 코드 검토·정리 (본 세션)

- 문법/미사용 함수/중복 선언 전수 점검 — 이상 없음
- redundant 필드 `paxBiz`/`paxEco` 제거 (paxNbr 인라인 조립으로 단순화)
- 매 import마다 OFP 전체를 콘솔에 덤프하던 `console.log('Parsed PDF Text', fullText)` 제거
- `updateDebugPanel`/`lastImportedPdfData`: 디버그 패널 DOM이 없어 no-op이나 `lastImportedPdfData`는 "PDF 로드됨" 플래그로 사용 중 → 유지
- 전 파서 회귀 테스트 통과 (AltnRoute/LOLV/ERA/FPL비교)

---

## 9. 다음 세션 후보

- NOTAM 페이지 고도화 (현재 경고문만 표시)
- `dev` → `main` 머지 시점 결정(실사용 배포)
- 필요 시 DRAWPAD localStorage 용량 관리(페이지 이미지 누적)
