# 작업 인수인계 — 2026-07-20

## 1. 작업 목적

OFP PDF를 IMPORT하면 WIND/TEMPERATURE SUMMARY의 데이터를 읽어 CRZ WIND 화면에 표시하고, STEP ALT와 비교해 고도별 바람 정보를 확인할 수 있도록 했다.

## 2. 적용된 파일과 커밋

- 핵심 코드: `app.js`
- 기능 설명: `WIND_DATA.md`
- Claude 인수인계: `CLAUDE.md`
- 기능 커밋: `c659093 Add OFP CRZ WIND data workflow`
- main merge 커밋: `e29a157 Merge dev into main`
- 방향별 고도 파서 커밋: `4fd06ec Support directional CRZ WIND altitude sets`
- 최신 캐시 갱신 커밋: `5e4da06 Refresh cache for directional wind parser`

현재 기준 브랜치는 `main`이며 `origin/main`과 동기화되어 있다. 계속 작업할 때는 `dev`에서 작업 후 검증하고 `main`에 merge하는 기존 흐름을 따른다.

## 3. UI 변경

- INIT 화면의 `STEP ALT` 버튼을 `STEP ALT/WIND`로 변경했다.
- 내부 탭은 `STEP ALT`와 `CRZ WIND`로 표시한다.
- CRZ WIND 화면은 waypoint 10개를 한 번에 표시한다.
- `▲▲`, `▼▼`, `RETURN` 버튼을 유지한다.
- waypoint 사이에 옅은 회색 구분선을 넣었다.
- 하단에는 `WIND/SAT · OFP {FROM} TO {TO}`가 표시된다.

## 4. WIND 파싱 규칙

함수 위치: `parseCrzWindData()`

- 대상 구간: `START OF WIND AND TEMPERATURE SUMMARY`부터 `DRIFTDOWN SUMMARY DATA` 전까지
- 대상 고도: FL330, FL350, FL370, FL390
- 입력 예: `25087+053-55`
- 출력: `250/087 -55`
- CMP는 화면에 표시하지 않는다.
- CMP 구분자는 `+`와 `-` 모두 허용한다.
- waypoint 라인은 WIND 고도 행 아래에 있으므로, 앞에서 수집한 고도 행을 다음 waypoint 라인에 연결한다.
- 반복된 dataplan 페이지의 중복 waypoint는 제거한다.
- `DISPATCH RELEASE INFORMATION` 이전의 첫 WIND SUMMARY만 사용한다.
- 후반부에 반복되는 WIND SUMMARY는 읽지 않는다.
- 3~5글자 waypoint와 좌표형 waypoint를 인식한다.

## 5. 좌표 waypoint 비교

STEP ALT와 CRZ WIND의 좌표 표기가 다를 수 있으므로 `normalizeWaypointCoordinate()`로 비교한다.

```text
65N160W -> 65N60
37N170E -> 37E70
37N180E -> 37E80
```

## 6. 색상 및 매칭 규칙

`isCrzWindHighlighted()`가 고도별·waypoint 순서별로 방향, 속도, SAT를 독립 비교한다.

- 방향 차이 30° 이상: 시안색
- 풍속 차이 30kt 이상: 시안색
- SAT 차이 5°C 이상: 시안색
- 하나라도 초과하면 해당 WIND/SAT 셀 전체를 시안색으로 표시
- 범위 내 값은 흰색
- 초과한 항목만 새 기준값으로 갱신

`isCrzWindStepAltMatch()`는 STEP ALT의 waypoint와 FL이 CRZ WIND의 waypoint/고도와 일치하는지 확인한다. 일치하는 해당 고도 셀은 시안/흰색 규칙보다 우선해 그린색으로 표시한다.

첫 번째 CRZ WIND waypoint의 첫 번째 고도는 STEP ALT의 첫 번째 유효 `FLxxx` 값을 사용한다.

## 6-1. ARINC 424 WGS84 좌표 축약 규칙

STEP ALT와 CRZ WIND의 좌표 표기가 달라도 아래 규칙으로 정규화한 뒤 같은 waypoint인지 비교한다. 경도 100도 미만은 경도 숫자를 그대로 사용하고, 100도 이상은 경도에서 100을 뺀 숫자와 해당 반구 문자의 위치 규칙을 적용한다.

```text
50N040W -> 5040N    30N160W -> 30N60
50N020E -> 5020E    50N120E -> 50E20
52S075W -> 5275W    52S160W -> 52W60
60S030E -> 6030S    60S130E -> 60S30
4880N    -> 48N080W와 동일 좌표
```

구현 위치는 `app.js`의 `normalizeWaypointCoordinate()`이며, 전체 표는 `WIND_DATA.md`를 참조한다.

## 7. Import/reset 동작

- `resetFlightData()`에서 `crzWindData`와 `crzWindScrollIndex`를 초기화한다.
- PDF 텍스트 추출 완료 직후 `parseCrzWindData(fullText)`를 실행한다.
- 새 PDF를 IMPORT할 때마다 이전 WIND DATA를 버리고 새 PDF 결과로 교체한다.
- 하드코딩 sample WIND 데이터는 제거했다.

## 8. 최근 검증

- `node --check app.js` 통과
- `git diff --check` 통과
- ImportantFile-1/6/7 PDF의 서로 다른 노선, 3글자 waypoint, 좌표 waypoint, CMP 부호 형식으로 파서 로직을 점검했다.
- ImportantFile-6의 AGSUS 기준값:

```text
FL330 300/090 -43
FL350 290/098 -47
FL370 290/092 -52
FL390 290/087 -58
```

## 9. 다음 작업 시 주의사항

- `app.js`는 빌드 도구 없이 직접 실행되는 단일 파일이므로 수정 후 반드시 `node --check app.js`를 실행한다.
- PDF 파서의 텍스트 순서가 OFP마다 달라질 수 있으므로 새 OFP를 추가해 회귀 확인한다.
- WIND SUMMARY의 waypoint 라인이 데이터 아래에 있다는 구조를 유지한다.
- 좌표 waypoint 비교 시 원문 문자열 직접 비교를 하지 말고 `normalizeWaypointCoordinate()`를 사용한다.
- 변경은 `dev`에서 검증 후 `main`에 merge한다.

## 10. 최종 점검 결과

- `node --check app.js` 통과
- `git diff --check` 통과
- `ImportantFile 6.pdf`: ICN→LAX 동쪽 고도 FL330/350/370/390 선택
- `ImportantFile 5.pdf`: JFK→ICN 서쪽 고도 FL320/340/360/380 선택
- `ImportantFile.pdf`: LAX→ICN 서쪽 고도 FL320/340/360/380 선택
- PDF 6→7페이지 경계의 WIND 블록을 하나의 데이터 블록으로 이어서 처리
- `52S160W -> 52W60` 수정 사항을 코드, 참조 문서, 인수인계 문서에 반영
