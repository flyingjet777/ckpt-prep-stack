# 작업노트 — 2026-06-26

> ckpt-prep-stack 개발 세션 정리 (Claude Code)
> 브랜치: `dev` → 검증 후 `main` 머지(GitHub Pages 운영 배포)

---

## 1. 배포 인프라 / 아이패드 PWA 안정화

| 항목 | 내용 |
|---|---|
| `.nojekyll` 추가 | GitHub Pages가 기본 Jekyll 처리를 시도하다 "Page build failed" 나던 문제 제거 → 빌드 `built` 정상화 |
| 캐시 버전 관리 | 프록시 전환 후에도 아이패드가 옛 app.js를 서빙하던 문제. `app.js?v`, `index.css?v`, SW `CACHE_NAME`을 변경할 때마다 올려 강제 갱신 |
| METAR/게이트 미표시 원인 | AeroDataBox API 문제 아님. 서비스워커(cache-first)가 프록시 도입 전 옛 코드를 캐시 서빙한 것 → 버전 올려 해결 |
| iOS 콜드오픈 축소 버그 | 백그라운드 복귀 시 `adjustBezelScale`을 1회만 호출 → viewport 안정화 전 작게 계산됨. `scheduleBezelScale`(즉시+100/300/600ms 재계산) + `orientationchange`/`visualViewport` 리스너 추가로 해결 |

---

## 2. 레이아웃/구조 수정

### NOTAM 페이지
- DEP/ARR NOTAM의 RETURN 버튼이 잘리던 문제: 이 페이지에만 있는 ETD/ETA 행 때문에 `.fms-content`(overflow:hidden)를 넘침. 테이블을 13→11행(`DEP_ARR_NOTAM_ROWS`)으로 줄여 높이를 맞춤 → RETURN이 `margin-top:auto`로 다른 NOTAM 페이지와 동일 위치 정렬. 스크롤 페이지네이션도 11행 단위로 일치.
- `.fms-content`에 `min-height:0; overflow:hidden` 추가 — 내용이 길어져도 푸터(MSG LIST)를 밀어내지 않도록 고정(전 페이지 공통 안전장치).

### DEP/ARR WX 페이지
- ARR WX의 RETURN 잘림: WX 테이블은 METAR 행 + TAF 행(`height:auto`로 긴 줄은 줄바꿈). ARR의 긴 TAF/METAR가 영역을 넘침 → `getDepArrWxTableHTML`의 TAF 표시를 10→8행으로 축소(`renderDepArrWxPage`의 스크롤 버튼은 `visibility:hidden`이라 페이지네이션 영향 없음).

### ALTERNATE RTEs
- 항로(route)와 LOLV 헤더 사이 빈 행(`spacer-2`)을 하나 더 추가해 2줄 더 띄움(가독성).

---

## 3. INIT(ACTIVE/INIT) 페이지 리디자인

- **Row 1 (FLT NBR / ACFT / APMS)**
  - `space-between` → `flex-start; gap:16px`로 왼쪽 정렬·균일 간격
  - FLT NBR 박스 80→100px (FROM 박스와 가로 위치 정렬: 그룹 gap 6→8px)
  - ACFT 박스 75→95px(APMS 근사), ACFT 라벨↔박스 간격 +4px(6→10)
  - IMPORT 버튼 제거(아래 Row 4로 이동)
- **Row 3 (CPNY RTE / RTE SEL)**: RTE SEL 옆에 흰색 **GATE** 라벨 + 녹색 게이트 버튼. 버튼은 IMPORT와 동일 폭(100px), 표시 형식 `T2  G266`(터미널+게이트). 클릭 시 재조회, 성공 녹색/실패 빨강/없음 회색.
- **Row 4 (ALTN RTE / ALTN RTE SEL)**: 우측 끝에 흰색 **CFP** 라벨 + **IMPORT** 버튼(폭 100px, `margin-left:auto`로 아래 CRZ TEMP 박스와 우측 끝·크기 정렬). IMPORT 테두리 2→4px로 강조.
- **FUEL&LOAD**: PAX NBR / CGO(T) / CI 박스 100→104px.

---

## 4. 신규 파싱 / 기능

- **CFP PLAN 번호**: OFP의 `CFP PLAN 6670` 파싱 → `flightData.cfpNbr`. 우측 상단 헤더를 `편명 / CFP NBR`(예: `AAR204 / 6670`)로 표시. CFP 번호 없으면 편명만.
- **GATE 표시**: `buildGateLabel()`이 터미널 있으면 `T{t}  G{g}`, 없으면 `G{g}`.

---

## 5. 핵심 파일 메모

- INIT 페이지: `renderInitPage()` (app.js)
- 헤더 갱신: `updateHeaderFltNbr()` — fltNbr + cfpNbr
- 게이트 라벨: `buildGateLabel()` / 게이트 조회 `fetchGate()`(프록시 경유)
- NOTAM 행 수: `renderNotamRows(lines, scrollIndex, rowCount=13)`, DEP/ARR는 `DEP_ARR_NOTAM_ROWS=11`
- 스케일: `adjustBezelScale()` / `scheduleBezelScale()`
- 캐시 버전 갱신 시: index.html의 `app.js?v` `index.css?v` + sw.js `CACHE_NAME` 함께 올릴 것
